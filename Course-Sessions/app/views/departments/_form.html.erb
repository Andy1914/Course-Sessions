<%#= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">  
    <%= f.label :personality_types %><br />
    <%= f.select :personality_types,Algorithm::PERSONALITY_TYPE.map{|k,v| [v,k]},{:selected => f.object.personality_types},:multiple=>true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<%# end %>
