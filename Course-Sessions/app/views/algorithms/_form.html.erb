<%= form_for(@algorithm) do |f| %>
  <% if @algorithm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@algorithm.errors.count, "error") %> prohibited this algorithm from being saved:</h2>

      <ul>
      <% @algorithm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :trait %><br />
    <%#= f.text_field :trait %>
    <%= f.select :trait,Algorithm::PERSONALITY_TYPE.map{|k,v| [v,k]},:selected => f.object.trait,:prompt => "Select a Trait " %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
